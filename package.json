{
  "dependencies": {
    "typescript": "^5.0.4",
    "vitest": "^0.30.1"
  },
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:run": "vitest run",
    "test:benchmark": "vitest bench"
  }
}
